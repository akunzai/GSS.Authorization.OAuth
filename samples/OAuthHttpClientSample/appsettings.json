{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  // Points at the oauth1 service in compose.yml. Credentials are RFC 5849's own
  // examples, matching OAUTH1_CLIENT_ID / OAUTH1_CLIENT_SECRET there.
  // TokenId / TokenSecret are issued by the three-legged flow: run
  // scripts/oauth1-token.sh to write them into appsettings.Development.json.
  "OAuth": {
    "ClientId": "dpf43f3p2l4k3l03",
    "ClientSecret": "kd94hf93k423kf44",
    "TokenId": "CHANGE_ME",
    "TokenSecret": "CHANGE_ME"
  },
  "Request": {
    "Method": "GET",
    "Accept": "application/json",
    "Uri": "http://oauth1:5000/api/me"
  }
}
